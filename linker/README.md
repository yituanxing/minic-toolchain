# MiniLD linker component

Planned public tool identity: `minic-ld`.

Ownership: symbol resolution, relocation application, section placement, linker-script
semantics within the supported surface, and final ELF/Image construction.

This component is reserved during M0 and has no production implementation yet.
Assembler parsing and archive construction remain separate owners.
