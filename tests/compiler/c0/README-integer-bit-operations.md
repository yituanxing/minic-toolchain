# Integer bit operations branch status

This temporary branch note records the bounded scope while the draft pull request is under validation.

- `<<` and `>>` longest-match tokens;
- shift precedence between additive and relational expressions;
- integer-only shift operands;
- signed arithmetic right shift and unsigned logical right shift on RV64;
- integer-only bitwise `&` with precedence above `^`;
- focused positive and negative fixtures;
- pinned tiny-AES frontier advancement.

This file must be removed or folded into durable project documentation before the pull request is marked ready.
