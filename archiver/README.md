# MiniAR archiver component

Planned public tool identity: `minic-ar`.

Ownership: archive-file construction and archive-member metadata. This component is
reserved during M0 and has no production implementation yet.

It may consume shared archive-format support when a second real consumer justifies
that extraction. It must not absorb linker policy.
