name: Linux Remaining Focused V0

on:
  push:
    branches:
      - "agent/linux-remaining-focused-v0"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: linux-remaining-focused-v0-${{ github.ref }}
  cancel-in-progress: true

jobs:
  remaining-focused:
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}

      - name: Install RV64 Linux prerequisites
        run: |
          sudo apt-get -o Acquire::Retries=3 update
          sudo apt-get -o Acquire::Retries=3 install -y --no-install-recommends \
            build-essential gcc-riscv64-linux-gnu binutils-riscv64-linux-gnu \
            bc bison flex libssl-dev libelf-dev make curl xz-utils python3

      - name: Build remaining Linux TUs and preserve intermediates
        shell: bash
        run: bash tools/ci/run-linux-remaining-focused-v0.sh

      - name: Preserve focused evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: linux-remaining-focused-v0
          path: build/linux-remaining-focused-v0/evidence/
          if-no-files-found: ignore
          retention-days: 5
