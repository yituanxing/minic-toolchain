name: PR102 Materialize Block Scope Extern Multi Declarators
on:
  push:
    branches: [agent/block-scope-extern-multi-declarator-v0]
permissions:
  contents: write
jobs:
  materialize:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with: {ref: agent/block-scope-extern-multi-declarator-v0, fetch-depth: 0}
      - name: Materialize block-scope extern multi-declarator semantics
        run: python3 tools/dev/pr102-materialize-block-extern-multi.py
      - name: Build and validate
        run: |
          set -Eeuo pipefail
          chmod +x tests/compiler/c0/run-block-scope-extern-multi-declarator.sh
          CLANG_FORMAT=clang-format-18 bash tools/maintenance/run-format.sh write
          git diff --check
          make -j4
          MINIC="$GITHUB_WORKSPACE/build/debug/bin/minic" BUILD_DIR="$GITHUB_WORKSPACE/build/focused" HOST_CC=cc sh tests/compiler/c0/run-block-scope-extern-multi-declarator.sh
          make check
      - name: Commit product source
        run: |
          git rm .github/workflows/pr102-materialize-block-extern-multi.yml tools/dev/pr102-materialize-block-extern-multi.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src tests
          git diff --cached --check
          git commit -m 'frontend: support block-scope extern multi-declarators'
          git push origin HEAD:agent/block-scope-extern-multi-declarator-v0
