name: Core M167b RV64 Sched Tail Trace

on:
  pull_request:
    paths:
      - 'src/**'
      - 'include/**'
      - 'Makefile'
      - 'tests/external/linux/corpus_replay.py'
      - 'tools/ci/apply-m161-rv64-int128-pair.py'
      - 'tools/ci/apply-m162-rv64-record-load.py'
      - 'tools/ci/apply-m163-rv64-caller-abi.py'
      - 'tools/ci/apply-m163b-rv64-zero-record-abi.py'
      - 'tools/ci/apply-m165-rv64-indirect-parameter-object.py'
      - 'tools/ci/apply-m166-rv64-fixed-asm-phase-alias.py'
      - 'tools/ci/apply-m167-rv64-zero-record-copy.py'
      - 'tools/ci/apply-m167b-rv64-instruction-trace.py'
      - '.github/workflows/core-m160-rv64-full500-census.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  core-rv64-sched-tail-trace:
    if: github.event_name == 'workflow_dispatch' || github.head_ref == 'refactor/declaration-sema-v1'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/declaration-sema-v1
          fetch-depth: 0

      - name: Record M167b trace head
        shell: bash
        run: |
          set -Eeuo pipefail
          echo "M166_STAGED_BASELINE_PASS=473"
          echo "M167B_TRACE_HEAD=$(git rev-parse HEAD)"
          test "$(git hash-object src/target/riscv64/core_codegen.c)" = a5414575a573abd9ce9ced00fb6c234d785e4e03

      - name: Materialize M161-M167 plus trace
        shell: bash
        run: |
          set -Eeuo pipefail
          python3 tools/ci/apply-m161-rv64-int128-pair.py
          python3 tools/ci/apply-m162-rv64-record-load.py
          python3 tools/ci/apply-m163-rv64-caller-abi.py
          python3 tools/ci/apply-m163b-rv64-zero-record-abi.py
          python3 tools/ci/apply-m165-rv64-indirect-parameter-object.py
          python3 tools/ci/apply-m166-rv64-fixed-asm-phase-alias.py
          python3 tools/ci/apply-m167-rv64-zero-record-copy.py
          python3 tools/ci/apply-m167b-rv64-instruction-trace.py
          git diff --check

      - name: Restore frozen Linux corpus
        id: linux-corpus-cache
        uses: actions/cache/restore@v4
        with:
          path: build/linux-focus-corpus
          key: linux-focus-corpus-v1-6.6.143-rv64-defconfig-gcc13.3.0-${{ hashFiles('tests/external/linux/replay-indices.txt') }}

      - name: Require frozen corpus
        shell: bash
        run: |
          set -Eeuo pipefail
          test "${{ steps.linux-corpus-cache.outputs.cache-hit }}" = true
          test -s build/linux-focus-corpus/selected-tus.txt

      - name: Build traced compiler
        run: make -j4 MODE=release CFLAGS=-Werror BUILD_DIR=build/m167b-compiler

      - name: Replay sched representative
        shell: bash
        run: |
          set -Eeuo pipefail
          compiler="$GITHUB_WORKSPACE/build/m167b-compiler/bin/minic"
          work="$GITHUB_WORKSPACE/build/m167b-trace/rq"
          set +e
          MINIC_CORE_IR=strict MINIC_CORE_CODEGEN=basic-v0 \
            python3 tests/external/linux/corpus_replay.py \
              --minic "$compiler" \
              --corpus "$GITHUB_WORKSPACE/build/linux-focus-corpus" \
              --work "$work" --jobs 1 --offset 96 --limit 1 --sample-count 0
          rc=$?
          set -e
          echo "M167B_FOCUSED index=96 rc=$rc"
          ! grep -R -F "Core-owned function 'rq_pin_lock' cannot be emitted by RV64 basic-v0" "$work/minic-stderr" 2>/dev/null
          grep -R -F "Core-owned function 'class_rq_lock_constructor' cannot be emitted by RV64 basic-v0" "$work/minic-stderr"
          grep -R -F "M167B_PREFLIGHT" "$work/minic-stderr"

      - name: Persist M167b trace
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: core-m167b-rv64-sched-tail-${{ github.sha }}
          path: build/m167b-trace
          if-no-files-found: warn
