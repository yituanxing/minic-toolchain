name: RV64 Callee ABI Location Validation

on:
  push:
    branches:
      - refactor/rv64-callee-abi-location-v1
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: rv64-callee-abi-location-${{ github.ref }}
  cancel-in-progress: true

jobs:
  host:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Materialize callee location consumer
        shell: bash
        run: |
          set -Eeuo pipefail
          python3 tools/dev/materialize-rv64-callee-abi-location-v1.py
          CLANG_FORMAT=clang-format-18 bash tools/maintenance/run-format.sh write
          git diff --check
          grep -F 'MinicRiscv64AbiCursor abi_cursor;' src/target/riscv64/codegen_function.c >/dev/null
          grep -F 'minic_riscv64_abi_place_argument' src/target/riscv64/codegen_function.c >/dev/null
          if grep -F 'size_t stack_parameter_index;' src/target/riscv64/codegen_function.c >/dev/null; then
            echo 'legacy callee stack parameter cursor remains' >&2
            exit 1
          fi
          git diff -- src/target/riscv64/codegen_function.c

      - name: Verify unchanged production source inventory
        run: sh tools/maintenance/check-production-source-inventory.sh

      - name: Run release host contracts
        run: make -j2 MODE=release BUILD_DIR=build/rv64-callee-abi-host CFLAGS=-Werror check-fast

      - name: Run sanitizer AST contracts
        run: make -j2 MODE=sanitize BUILD_DIR=build/rv64-callee-abi-sanitize check-ast-contract

      - name: Run frozen Foundation focused semantics
        shell: bash
        env:
          MINIC: ${{ github.workspace }}/build/rv64-callee-abi-host/bin/minic
          BUILD_DIR: ${{ github.workspace }}/build/rv64-callee-abi-foundation-focused
          HOST_CC: cc
        run: sh tools/dev/pr76-focused.sh

  full:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      DEBIAN_FRONTEND: noninteractive
    steps:
      - uses: actions/checkout@v4

      - name: Materialize callee location consumer
        shell: bash
        run: |
          set -Eeuo pipefail
          python3 tools/dev/materialize-rv64-callee-abi-location-v1.py
          CLANG_FORMAT=clang-format-18 bash tools/maintenance/run-format.sh write
          git diff --check

      - name: Restore RISC-V package archives
        id: rv64-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/minic-apt/archives
          key: rv64-apt-${{ runner.os }}-${{ runner.arch }}-noble-${{ hashFiles('tools/ci/ubuntu-24.04-packages.txt') }}

      - name: Run complete compiler gate
        env:
          RV64_CACHE_HIT: ${{ steps.rv64-cache.outputs.cache-hit }}
        run: bash .github/scripts/compiler-c0-full-gate.sh
