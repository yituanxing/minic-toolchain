# Object utilities

Reserved future tools include `minic-nm`, `minic-objdump`, `minic-objcopy`, and
`minic-strip`.

These tools are not part of MiniAS V1. They are useful future second consumers of
ELF/archive/RISC-V factual libraries and therefore help reveal the correct shared
boundaries when implemented.
